{
  "name": "sekety-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preinstall": "rm -rf node_modules package-lock.json || true",
    "dev": "vite",
    "build": "chmod +x ./node_modules/.bin/vite && vite build",
    "preview": "vite preview",
    "vercel-build": "npm ci && npm run build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
